{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Edition d'un utilisateur</h1>
            <br>
            <br>
        {{ include('user/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

        {% if is_granted("ROLE_ADMIN") %}        
            <a class="btn btn-primary ml-4" href="{{ path('app_user_index') }}">Retour a la liste</a>
            {% else %}
            <a class="btn btn-primary ml-4" href="{{path('app_profil') }}">Retour au profil</a>
        {% endif %}
        {{ include('user/_delete_form.html.twig') }}
        
    </div>
{% endblock %}

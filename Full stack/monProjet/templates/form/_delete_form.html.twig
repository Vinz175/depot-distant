<form method="post" action="{{ path('app_form_delete', {'id': disc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ disc.id) }}">
    <br>
    <button class="btn btn-danger ml-4">Supprimer</button>
</form>
